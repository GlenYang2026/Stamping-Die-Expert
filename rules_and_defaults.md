# rules_and_defaults.md —— 冲压常用规则与默认值

> ⚠️ 所有数值均为**典型经验值**，实际以材料证书、模具厂工艺卡、现场试模为准。
> 本文件数值用于估算与方案比选，不替代正式工艺文件。

## 一、刀口间隙（冲裁间隙）

**口径约定：本技能所有"间隙"均指双边总间隙（= 凹模尺寸 − 凸模尺寸），即两面之和。**
（部分教材用"单边间隙"= 双边/2，混用时务必换算，避免选错间隙导致毛刺/断面质量失控。）

| 材料类别 | 软态 / 退火 | 半硬 / 硬态 | 备注 |
|---|---|---|---|
| 铜 T2 / TU2（软） | 4% ~ 8% t | 6% ~ 10% t | 软铜延展好，可取小间隙 |
| 黄铜 H62（软） | 5% ~ 9% t | 8% ~ 12% t | |
| 铝 1050/3003/5052（软） | 4% ~ 8% t | 6% ~ 10% t | 软铝间隙可更小 |
| 低碳钢 SPCC/DC01 | 6% ~ 10% t | 8% ~ 12% t | |
| 镀锌板 SECC/SGCC | 6% ~ 10% t | 8% ~ 12% t | 涂层侧略放大 |
| 不锈钢 304 | 6% ~ 10% t | 8% ~ 12% t | 加工硬化强，硬态取大 |
| 不锈钢 301（弹片） | 8% ~ 12% t | 10% ~ 14% t | 硬态难冲，间隙放大 |
| 硅钢（定转子） | 5% ~ 9% t | — | 薄料、要求毛刺极小，间隙从严 |
| 磷青铜 C5191 | 6% ~ 10% t | 9% ~ 13% t | |
| 高强钢（DP/HSLA） | 8% ~ 12% t | 10% ~ 14% t | 力大、磨损快，需耐磨凹模 |

示例：T2 软铜，t = 2.0 mm，双边间隙建议 0.08 ~ 0.16 mm（即单边 0.04 ~ 0.08 mm）。
旧版文档曾把 6%~12% 按"单边"解释，导致双边达 12%~24%，偏保守且易误用——已统一为双边口径。

## 二、剪切强度系数 tau_factor（τ = tau_factor × UTS）

| 材料 | tau_factor | 说明 |
|---|---|---|
| 铜 / 铝（软） | 0.70 | 软态有色金属韧性大，落料/冲孔取 0.70 适配 |
| 不锈钢 | 0.70 ~ 0.75 | 取 0.72 常用，兼顾冷作硬化 |
| 低碳钢 / 镀锌钢 | 0.80 | 钢剪切强度普遍偏高 |
| 硬钢 / 淬火钢 | 0.80 ~ 0.85 | 高强度/淬火态可取上限 0.85，普通冷轧统一 0.80 |
| 高强钢 | 0.75 ~ 0.80 | |

冲裁力：`F_shear = L × t × τ`（N），`L` 为剪切周长（mm），`t` 板厚（mm），`τ` 单位 MPa = N/mm²。
吨位换算：`1 tf = 9806.65 N`，`F_ton = F_N / 9806.65`。

## 三、总冲压力（选机台必须用总力，不能只算纯冲裁力）

实际冲床需克服：冲裁力 + 卸料力 + 推件力（+ 顶件力，复合模才显著）。

- 卸料力 `F_strip ≈ strip_pct × F_shear`
- 推件力 `F_eject ≈ eject_pct × F_shear`
- 总力 `F_total = F_shear × (1 + strip_pct + eject_pct)`

| 材料 | 卸料力 strip_pct | 推件力 eject_pct |
|---|---|---|
| 钢（SPCC/SECC/高强钢） | 0.05 ~ 0.10 | 0.03 ~ 0.08 |
| 不锈钢 | 0.06 ~ 0.10 | 0.04 ~ 0.08 |
| 铜 / 铝 | 0.03 ~ 0.08 | 0.02 ~ 0.06 |
| 硅钢（薄料） | 0.04 ~ 0.07 | 0.03 ~ 0.05 |

> 只按纯冲裁力选机台会**偏小**，连续生产有压塌/过载风险。保守估算用上表上限。

**卸料 / 推件力比例落地细化规则（与材料库一致）：**
- 薄料（t < 0.8 mm）：材料易抱紧冲头，取区间**上限**；
- 厚硬料（t ≥ 2 mm 或硬态）：同样取**上限**；
- 软铜 / 软铝：韧性好、抱料力偏弱，整体比钢材**低一档**；
- 高速连续模：统一取区间**中间值**；
- 手工单冲 / 试制：取**上限**更稳妥。

## 四、安全系数与机台档位

- 默认安全系数 `sf = 1.25`（薄料、精密、高速可适当提到 1.3~1.5）。
- 推荐机台吨位 `≥ F_total_ton × sf`，向上取整到常用档位：
  **5 / 10 / 20 / 30 / 50 / 60 / 80 / 100 / 160 / 200 / 250 / 315 t**（国标系列，取就近≥值）。

## 五、搭边与步距（排样，详见 nesting_calculator.csv）

- 搭边 `a`（侧搭边）、`a1`（步距间搭边）一般取 `(1.5 ~ 3) × t`，薄料取小、厚料取大；自动送料可取小值。
- 料宽 `B = 零件最大宽 + 2a (+ 侧刃余量)`；步距 `P = 零件送料方向长 + a1`。
- 利用率 `η = 单件面积 / (P × B)`（单排）；双排/斜排另算。

## 六、弯曲（详见 process_formulas.md）

- **弯曲力系数 C 按材质分 4 档**（见 materials_db.csv `bending_c` 列，正式工艺卡必须分型，粗算可用 1.33）：
  - 普通冷轧钢 90° 自由弯曲：**1.33**
  - T2 铜 / 软铝：**1.15 ~ 1.20**（塑性好，系数下调）
  - 软态 304 不锈钢：**1.45 ~ 1.50**（回弹大、变形抗力高）
  - 厚料 t ≥ 2.0 mm：**所有材料系数再 +0.08 ~ 0.12**
- 最小弯曲半径 `R_min` 见 materials_db.csv（软铝/铜可近 0t，304 约 1.0t，301 约 2~3t）。
  - **库值 R_min 为横纹（⊥纤维方向）安全值**；顺纹（∥纤维）折弯易裂，`R_min` 需放大 **1.5~2 倍**，或改横纹排样。
  - t > 3 mm 厚板按成形手册再酌情放大。
- 中性层系数 `k`：r/t 大时趋近 0.5，小时 0.3~0.45（见弯曲公式文档）。
- 回弹随屈服强度↑、料厚↓而增大，硬态材料需过弯补偿。

## 七、拉深（详见 process_formulas.md）

- 首次拉深比 `m1 = d/D`，钢/不锈钢约 0.50~0.55，铝/铜约 0.55~0.62；超过需多次拉深。
- **304 首次拉深极限双标准（关键）：**
  - **量产稳定标准（模具定稿用）：首次 m ≥ 0.53 锁死**。304 加工硬化极强，m=0.50 属临界极限，受料带硬度波动、润滑油、压边力、料厚公差影响大，量产极易批量拉裂，不能作为量产门槛。
  - **研发试模特例：允许临时下探至 m=0.50**，但必须强制备注"极限试模、禁止批量量产"，并配套退火 / 改善润滑 / 加大压边 / 放大凹模圆角。
  - 脚本 `drawing` 命令默认按量产 0.53 校核；加 `--rd` 进入研发试模模式（放宽至 0.50 并强提示禁止量产）。
- 压边力 `F_bh` 一般取拉深力的 2%~5%（或按法兰单位压边力 1~3 MPa 估算）。
