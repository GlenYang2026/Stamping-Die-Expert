# 材料库校正说明（按现场来料质保书校正）

> 由老杨（MoldYang）落地方案整理。本说明解决一个问题：**材料库里的数值是"典型参考值"，不是你车间在用料号的真实值。**
> 正式量产工艺卡 / 报价锁价前，必须用采购来料质保书实测值替换。以下给出可操作流程与填表模板。

---

## 一、为什么必须校正（不能直接用通用牌号）

通用牌号（"SPCC""304""5052"）的 UTS / 屈服 / 最小弯曲半径波动极大：

1. **钢材**：冷轧深冲料（SPCD/SPCE）与普通冷轧料（SPCC）屈服、抗拉差距明显；
2. **铜**：T2 硬态 / 半硬 / 软态，屈服可差 1 倍以上；
3. **铝**：1060、3003、5052 屈服与成形性完全不同，不能混为一类；
4. **304 不锈钢**：2B 软态（屈服约 205 MPa）、半硬（可达 400+ MPa）、BA 光亮料，差距巨大；硬态折弯极易开裂，R 须比软态放大一倍。

→ **结论：以采购来料质保书参数为准，不用书本通用均值。**

---

## 二、校正操作流程

1. **整理车间高频料号清单**：材质 + 硬度状态（软/半硬/硬、2B/BA）+ 常用料厚区间。
2. **每个料号录入质保书实测值**：UTS、屈服强度（抗拉/屈服是换算 τ、弯曲力、回弹的核心）。
3. **最小弯曲半径分两套**：
   - **横纹（弯曲线 ⊥ 轧制纤维方向）**：库值 `min_bend_radius_t` 即此值；
   - **顺纹（弯曲线 ∥ 轧制纤维方向）**：易裂，取横纹的 **1.5~2 倍**，或在排样时强制改横纹。
   - 入库建议在 `notes` 标注"横纹 Xt / 顺纹 (1.5~2)Xt"。
4. **料厚分区**：t<1 mm、t 1~3 mm、t>3 mm 的 R_min 不同，厚板按成形手册再放大。
5. **`data_source` 列**：校正后把该行从 `参考值` 改为 `质保书-批号/日期`，便于追溯。

---

## 三、填表模板（= materials_db.csv 列，复制后填实测值）

| 列 | 含义 | 填法 |
|---|---|---|
| material | 料号（含硬度，如 `T2_H`、`304_1/2H`） | 自定义，脚本按此查询 |
| grade | 英文牌号 | 如 Copper / Stainless |
| category | 中文类别 | 如 铜(硬) |
| UTS_MPa | 抗拉强度 | **质保书实测** |
| yield_MPa | 屈服强度 | **质保书实测** |
| tau_factor | 剪切系数 | 钢0.80(硬钢0.85)/铜铝0.70/304 0.72 |
| tau_MPa | 剪切强度 | = UTS×tau_factor（自动算，可留空后脚本算） |
| density_g_cm3 | 密度 | 铜8.9/铝2.7/钢7.85/304 7.93 |
| elongation_pct | 延伸率 | 质保书 |
| clearance_double_pct_soft | 软态双边间隙%t | 见 rules 间隙表 |
| clearance_double_pct_hard | 硬态双边间隙%t | 见 rules 间隙表 |
| min_bend_radius_t | 最小弯曲半径(倍t, 横纹) | 质保书/手册 |
| strip_pct | 卸料力比例 | 钢0.05~0.10/铜铝0.03~0.08 |
| eject_pct | 推件力比例 | 钢0.03~0.08/铜铝0.02~0.06 |
| bending_c | 弯曲力系数(4档) | 钢1.33/铜铝1.15~1.20/304 1.45~1.50/厚料+0.10 |
| data_source | 数据来源 | `参考值` 或 `质保书-批号` |
| notes | 备注 | 横纹/顺纹 R、硬度说明等 |

**示例（把通用 T2 软行替换为你的实测料号）：**
```
T2_R180, Copper, 铜排T2(软,来料批A), 205, 52, 0.70, 144, 8.94, 46, 4, 8, 0.1, 0.05, 0.04, 1.18, 质保书-2026A-批01, "横纹0.1t/顺纹0.2t"
```

---

## 四、录入方式

- 直接编辑 `materials_db.csv` 追加行（保持表头顺序）；
- 或在脚本里用 `shear --tau_factor ...` / `bending --sigma ...` 临时覆盖；
- 新增料号后，`stamping_calc.py` 的 `shear/bending/drawing` 会自动按 `material` 名匹配。

> 未校正的行保持 `data_source=参考值`，仅用于快速估算；锁价/定稿前务必替换为质保书行。
